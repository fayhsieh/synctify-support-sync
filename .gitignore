# ── 憑證與機敏資料（絕對不可提交）──────────────────
.env
.env.*
!.env.example
*credential*
*secret*
*password*
*.pem
*.key

# n8n workflow 匯出檔可能夾帶憑證，確認後再手動解除忽略
n8n/*-with-credentials.json
# 帶真實 webhook path 的本機產出（build_n8n_code_node.py --local）
n8n/local/

# ── 轉換器輸出（可重新產生，不進版控）──────────────
converter/output/
*-output.json
faq-items.json
conversion-report.json
elementor-template-output.json

# ── WP 端備份／匯出 ──────────────────────────────
*.sql
wp-content/

# ── 術語表備份（scripts/glossary_backup.py）──────────
# 術語表是公司內部資料（備註有內部討論與待修的產品問題），這個 repo 是公開的，絕對不可提交
backups/

# ── Python ──────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# ── 編輯器與系統 ────────────────────────────────
.DS_Store
.vscode/
.idea/
*.swp
